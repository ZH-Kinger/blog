---
title: Positional Encoding（位置编码）
icon: server
date: 2026-07-13
order: 113
category:
  - AI基础设施
tag:
  - AI Infra
---

在大模型（Transformer 架构）的世界里，**Positional Encoding（位置编码）** 是赋予大模型“空间感”与“文字顺序感知能力”的物理金钥匙。

简单来说：**如果没有位置编码，你把一句话打乱顺序喂给大模型，大模型算出来的结果是完全一模一样的。它分不清“我通过了面试”和“面试通过了我”。**

为了理解为什么需要它、它的数学本质以及现代大模型（如 Llama 3）是怎么做的，我们直接扒开它的物理真相：

---

### 一、 传统网络 vs Transformer 的“先天残疾”

- **传统的 RNN / LSTM 网络**：天生具备时序感。因为数据是一个词接一个词、按时间顺序“流”进网络的。前一个词不跑完，后一个词进不来。
- **Transformer 架构**：为了极大地压榨 GPU 的并发算力，它抛弃了这种死板的串行流，采用 **Self-Attention（自注意力机制）**。它把一句话里的所有单词（无论是一百个字还是一万个字）**在同一个时间步、齐刷刷地一次性全部并行塞进显存暴算**。

但是这种极致的并行带来了一个致命的物理副作用：自注意力机制在算两个词之间的相关性时，采用的是点积（Dot Product）操作。而点积在数学上是具有**交换律**的。  
也就是说，在纯 Transformer 的世界观里，它只知道这句话里有哪些词（像一个装满词的袋子），但**完全分不清这些词谁在前、谁在后**。这种残疾被称为“排列不变性（Permutation Invariance）”。

---

### 二、 位置编码的物理本质：给每个词发一张“座位号”

为了解决这个残疾，科学家想出了一个绝妙的做法：既然文字序列本身没有顺序，那我们就人为创造一套**代表物理位置的特征向量**，把每个词在句子里的“绝对座位号”或者“相对距离”强行揉进数据里。

在工程演进中，主要出现了两大派系的位置编码技术：

#### 派系 1：经典的“绝对位置编码”（以 GPT-2、BERT 为代表）

在 Transformer 最早期的论文（*Attention Is All You Need*）中，采用的是 **正弦/余弦静态编码（Sinusoidal Positional Encoding）**。

- **做法**：算法利用不同频率的正弦和余弦函数，生成一个与单词向量（Embedding）维度完全一样的位置向量 ![](https://cdn.nlark.com/yuque/__latex/ffd1905f6d4d60accedfa6b91be93ea9.svg)。
- **物理操作**：在数据刚进模型的第一层，直接将**词向量**与**位置向量**进行**物理相加（Plus）**：

![](https://cdn.nlark.com/yuque/__latex/e359b071bdf2edcf48d18b38a46a4c4e.svg)

- **数学美感**：因为三角函数的数学特性（如 ![](https://cdn.nlark.com/yuque/__latex/45d3644fec2591f07e6b2fcb1f925555.svg) 的展开），这种编码方式可以让模型仅仅通过两点之间的点积，就能轻松学到“词 A 和词 B 之间物理距离是 3 个字”这样的几何关系。

后来，GPT-2 等模型把这种静态函数改为了 **Learnable Absolute Positional Encoding（可学习的绝对位置编码）**，即直接在内存里开辟一块 `[Max_Seq_Len, Hidden_Size]` 的参数矩阵，让模型在训练过程中自己去摸索和进化每个座位的最优特征。

---

### 三、 现代大模型的绝对主流：RoPE 旋转位置编码（相对位置）

传统的绝对位置编码有一个致命的物理硬伤：**它锁死了模型的最大文本长度。** 如果你训练时位置编码矩阵只设了 2048，那模型这辈子就只能读 2048 个字，遇到 4096 的长文本，多出来的座位直接没有“座位号”了。

因此，以 Llama、Mistral、Qwen 等为代表的现代大模型，全面抛弃了绝对位置编码，改用目前 Infra 界封神的 **RoPE（Rotary Position Embedding，旋转位置编码）**。

#### 💡 RoPE 是怎么玩的？（硬核物理拆解）

RoPE 彻底颠覆了“直接相加”的死板做法，它巧妙地借用了**复数几何空间和二维旋转矩阵**：

```
     【 传统的绝对编码 】                         【 现代的 RoPE 旋转编码 】
  ┌───────────────────────┐                    ┌───────────────────────────────────┐
  │  词向量 ⊕ 位置向量     │                    │  让词向量在二维高维空间上 “转圈”    │
  │ (在原始维度上暴力相加)  │                    │ (通过旋转角度 θ 物理绑定相对距离)     │
  └───────────────────────┘                    └───────────────────────────────────┘
```

1. **空间旋转**：模型在每一层算自注意力之前，把查询向量 ![](https://cdn.nlark.com/yuque/__latex/4ef7132d0df72d9e3db76f6391960a3d.svg) 和键向量 ![](https://cdn.nlark.com/yuque/__latex/38a3f4d664b7a723d138f9d57be0c783.svg) 切成很多个两两一组的二维平面。
2. **乘以旋转矩阵**：如果一个词处在第 ![](https://cdn.nlark.com/yuque/__latex/4760e2f007e23d820825ba241c47ce3b.svg) 个位置，RoPE 就会让这个词的向量在二维平面上**物理逆时针旋转** ![](https://cdn.nlark.com/yuque/__latex/b1b125bb9d5e62304671fb87cb7d39b5.svg) **个角度**。如果另一个词在第 ![](https://cdn.nlark.com/yuque/__latex/df378375e7693bdcf9535661c023c02e.svg) 个位置，它就旋转 ![](https://cdn.nlark.com/yuque/__latex/297e6fef06e177c188d2ba192ef268d7.svg) 个角度。
3. **内嵌相对距离**：当这两个词在计算 Attention 点积（![](https://cdn.nlark.com/yuque/__latex/c38a7522e9e20aace79fa2d9380c1960.svg)）时，奇妙的数学魔法发生了：由于两个向量各自旋转后的点积，其结果**只取决于它们两个旋转角度的差值（即** ![](https://cdn.nlark.com/yuque/__latex/869b600d0e1f76389953389372226f26.svg)**）**！
4. **长文本外推（Extrapolation）的基石**：RoPE 完美地将“绝对位置”转化为了“两词之间的相对距离”。这使得现代大模型具备了极其恐怖的**外推能力**。当我们需要将大模型从 8K 上下文扩展到 128K 甚至 1M 时，工程师只需要在底层把旋转的弧度 ![](https://cdn.nlark.com/yuque/__latex/ed5a4aa5e092e303a69c608582c70db9.svg) 稍微做一下动态缩放（如 Linear Scaling、Yarn 或 NTK-aware 缩放算子），不需要重新训练整个模型，就能让大模型直接吞下超级长文本。

### 💡 极简总结：

**Positional Encoding 就是 Transformer 架构里的“空间坐标系”。** 它通过数学算子（早期相加，现代旋转）将文字在句子中的物理先后顺序灌进向量里，从而让天生只懂并发暴算、缺乏时序感的 Transformer 重新获得了完美的语言逻辑和上下文顺序感知能力。
